{
  "name": "charity-app",
  "private": true,
  "scripts": {
    "start:ia": "cross-env node public/server.js",
    "deploy:functions": "firebase deploy --only functions",
    "deploy:hosting": "firebase deploy --only hosting",
    "deploy": "firebase deploy --only functions,hosting"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "firebase": "^12.0.0",
    "node-fetch": "^3.3.2",
    "cross-env": "^7.0.3"
  }
}
